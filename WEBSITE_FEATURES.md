# ALTS / ARKLINK Terminal Service 网站功能介绍

作者：梵音

## 一、网站定位

ALTS / ARKLINK Terminal Service 是一套面向《行于泰拉》TRPG 的在线跑团辅助平台。网站将角色卡创建、规则资料、地图编辑、多人房间、战术棋盘、掷骰记录和房间管理整合到同一套终端界面中，让玩家可以集中维护角色资料，让 SN 可以准备地图、管理单位并组织线上游戏。

平台采用账号制保存数据，并通过实时通信同步房间内的棋盘、TOKEN、聊天、掷骰和音乐状态。电脑端提供完整操作界面，手机和平板端提供紧凑的房间工具栏；低配置设备还可以开启低配机适应模式，减少动画与渲染开销。

## 二、账号与全局功能

### 1. 用户系统

- 支持玩家注册、登录和退出登录。
- 登录状态保存在当前浏览器中，后续访问可继续使用自己的角色卡、地图和房间数据。
- 区分 SN 与普通玩家身份，并在房间内进一步管理具体操作权限。

### 2. 主页与导航

- 主页提供角色卡和游戏房间的快捷入口。
- 左侧导航可进入角色、房间、地图、规则和速查页面。
- 提供可拖动、缩放和最小化的浮动窗口，用于音乐、PDF 规则书和终端使用说明。
- 主页右下角设有作者支持入口，确认提示后可查看打赏图片。

### 3. 背景音乐

- 登录页、大厅和房间分别使用独立的音乐场景。
- 支持播放、暂停、上一首、下一首、进度跳转、音量、静音、单曲循环和列表循环。
- 可以按分组管理曲目，并在房间中添加本地音频。
- 进入房间后会停止大厅音乐，避免两个音乐场景同时播放。
- 房间音乐默认由 SN 控制；SN 可以向玩家授权，也可以随时收回权限。

### 4. 性能与设备适配

- 支持电脑、手机和平板的响应式布局。
- 房间移动端提供角色/聊天、骰子和背包快捷入口。
- 低配机适应模式会降低画布像素密度、动画数量和部分视觉效果的开销。
- 页面采用按需加载，角色卡、房间和地图编辑器不会在首次进入时全部加载。

## 三、角色卡系统

### 1. 创建与管理

- 玩家可以直接新建角色卡，也可以删除已有角色卡。
- 角色编辑内容支持自动保存，同时保留手动保存入口和保存状态提示。
- 角色列表可展示角色头像、名称和主要档案信息。
- 支持上传角色肖像，并在独立调整窗口中通过拖拽移动、滚轮缩放，自由设置立绘构图。

### 2. Excel 导入与导出

- 在角色卡编辑页右上角导入 V1.2 Excel 角色卡。
- 导入基础档案、生日、属性、技能、职业、战斗数据、感染、特质、源石技艺、武器、防具、配件、投掷物、消耗品和信誉数据。
- 对 Excel 中已经计算过的特质修正进行归一化，避免网页端再次计算造成重复叠加。
- 可以将网站中的角色卡反向导出为 V1.2 Excel 模板，保留模板结构、公式和工作表。

### 3. 基础档案与人物资料

- 记录姓名、玩家、性别、年龄、生日、出身、种族、职业、子职业、身高、体重和等级等信息。
- 档案资料包含出身家庭、人生经历和个人现状。
- 个人信念包含角色性格、处世观念和对感染者态度。
- 另有客观履历、个人信念、随身物品和备注等纯文本资料接口。

### 4. 等级、属性与自动计算

- 总等级范围为 1 至 15 级。
- 精英化阶段由等级自动确定：1 至 5 级为精英化 0，6 至 10 级为精英化 1，11 至 15 级为精英化 2。
- 管理七项基础属性，并区分基础值、手动修正、成长值和特质修正。
- 自动计算生命值、技力上限、初始技力、耐力、物理抗性和法术抗性。
- 公式计算结果不会强制锁死输入；玩家可以手动调整，数值与公式不一致时会显示提醒。
- 物理抗性和法术抗性按职业公式及所选通识技能计算，并按规则向下取整。

### 5. 战斗职业与子职业

- 选择职业后可以查看分支说明，再确认选择对应子职业。
- 子职业资料包含分支特性、武器类型、SP 回复、攻击范围、防具类型、生命类型及抗性算法。
- 根据 V1.2 战斗职业表展示各精英化阶段的天赋和职业技能。
- 职业技能与源石技艺使用相应点数进行学习和管理。
- 吟游者武器可以选择个人魅力作为依赖属性，并可选择声乐作为攻击技能。

### 6. 技能系统

- 七项基础属性分别关联自己的技能表。
- 区分战斗流派、非战斗流派、核心技能、普通技能和特质附加技能。
- 战斗流派在建卡时拥有基础分配点，并可选择一项流派获得额外点数。
- 非战斗流派的加点池由所属属性及流派修正共同决定。
- 核心技能值跟随该战斗流派普通技能的最大值，并叠加只作用于具体技能的修正。
- 加点受到精英化阶段上限限制，具体小技能的特质修正可以独立超过加点上限。
- 界面分别展示加点、手动修正和特质修正，避免不同来源混入同一栏。

### 7. 特质系统

- 特质统一分为主要特质、次要特质、感染特质和社交特质。
- 主要特质包含种族、文化和工作，角色必须且只能各选择一个。
- 初始拥有 40 点特质点和 80 点特质点消耗上限。
- 正费用特质消耗特质点，负费用特质返还特质点，但不会提高 80 点消耗上限。
- 社交特质独立消耗社交点；社交点支持手动输入和一次性骰子生成，并保留投掷结果作为参考。
- 感染特质不消耗普通特质点，每 20 点感染值获得一个选择额度。
- 支持绑定特质、上级特质、修正因子、费用折扣和选择性效果。
- 特质造成的属性、流派、技能、等级成长和附加技能修正会自动进入角色计算。
- 支持手动编写特质，同时保留从 Excel 和规则库导入的词条。

### 8. 感染系统

- 记录免疫力、初始感染值、当前感染值、感染阶段、症状、临时发作和长期疗程。
- 感染阶段随感染值自动变化，并提供每个选项的规则说明。
- 症状选择弹窗可展示常驻修正与临时发作效果。
- 感染值会参与感染特质可选数量的计算。
- 在游戏房间内也可以修改角色感染数据，无需退出房间。

### 9. 源石技艺与职业技能

- 按源石技艺适应性的小类与大类关系选择流派。
- 光明和暗影归入咒法化形类。
- 支持选择流派、学习主动技艺、被动技艺和战斗职业技能。
- 被动技艺消耗技艺点，并与战斗职业天赋区分展示。
- 根据精英化阶段、技艺点和前置条件管理可学习内容。
- 支持从 V1.2 源石技艺 Excel 导入角色已选流派和技艺。
- 模组阶段已预留数据接口，当前不开放选择。

### 10. 武器、装备与信誉

- 装备分为武器、防具、武器配件、防御配件、投掷物、消耗品和召唤物。
- 武器记录名称、所属分支、防御技能、骰面、攻击类型、伤害类型、依赖属性、攻击技能和描述。
- 自动计算武器效能骰与辅助骰。
- 防具记录名称、依赖技能、防具类型和描述。
- 投掷物、消耗品和配件支持规则库选择、Excel 导入及手动添加。
- 召唤物保留独立接口，便于后续扩展完整规则。
- 信誉系统包含经济、资源、人脉和头衔四栏。
- 资源中的源石锭会扣除配件、投掷物和消耗品的价值，并显示剩余资源。
- 人脉和头衔共享相应的社交资源预算并提供支出提示。

### 11. 建卡掷骰与分享

- 角色卡页面提供独立的建卡掷骰记录。
- “属性天命”可以一次投掷 7 次 2D4，并将每次结果保存在角色卡中。
- 可以生成角色卡公开分享链接，其他人无需编辑权限即可查看完整角色卡。
- 分享内容会随角色卡后续保存同步更新。
- 可以重新生成链接使旧链接失效，也可以直接撤销分享。

## 四、规则资料系统

### 1. 网页规则库

- 浏览种族、文化、特质、战斗职业、源石技艺和感染系统规则。
- 职业页面展示职业分支、装备、算法、天赋和技能信息。
- 源石技艺页面按流派和阶段组织规则数据。

### 2. PDF 规则书

- 通过浮动窗口直接阅读项目内置 PDF 规则书。
- 阅读窗口可以拖动、缩放、最小化，并可在查看规则时继续操作其他页面。

### 3. 战斗速查与术语提示

- 战斗速查页面收录主动动作、正面状态、负面状态、六类损伤爆发和地形效果等资料。
- 当前术语解释重点覆盖正面状态、负面状态和六类损伤爆发。
- 文本中使用引号或【】标记的术语可以高亮并在悬停时显示说明。
- 正面效果使用绿色、负面效果使用红色，其他术语使用橙色。
- 遇到嵌套术语时，提示框会同时展示相关下级术语解释。

## 五、游戏房间

### 1. 创建、加入与角色绑定

- SN 可以设置房间名称、描述和最大玩家人数，并获得邀请码。
- 玩家通过邀请码加入房间，可以在进入前选择角色卡，也可以不绑定角色直接进入。
- 在房间成员区域可以更换当前绑定角色。
- 使用玩家角色创建 TOKEN 时，会自动将该 TOKEN 的控制权分配给对应玩家。

### 2. 房间生命周期与存档

- 房间内无人在线后不会继续显示在大厅，而会保存为房间存档。
- 存档保留棋盘、TOKEN、场景、地形、房间道具库和聊天记录等内容。
- 创建房间时可以新开空白房间，也可以选择历史存档恢复游戏。
- 存档显示所有玩家上次退出后的归档时间，并支持删除。
- SN 可以手动保存房间；SN 退出前也会触发自动保存。
- SN 可以将主持权限转让给其他房间成员。

### 3. 成员与权限

- TOKEN、地图素材、场景素材和网格设置按权限独立校验。
- 玩家只能操作自己拥有控制权的 TOKEN；被授权后可以移动并修改生命、技力和耐力等属性。
- SN 可以授予或收回控制权，并可统一操作所有有权限的目标。
- 房间音乐控制权可单独授权和收回，不影响 TOKEN 或地图权限。
- 权限同时在前端交互层和服务端实时事件层校验，避免只隐藏按钮却仍可发送修改。

### 4. 房间角色卡查看与修改

- 成员列表可以查看自己及同房间其他成员正在使用的完整角色卡。
- 角色卡在独立窗口中打开，展示内容与公开分享页面一致，不遮断房间内其他操作。
- 角色本人或 SN 可以在房间内修改允许变更的角色数据，例如感染值和信誉系统。
- 房间内角色变更通过统一接口保存回角色卡，为后续升级和感染阶段变化预留扩展能力。

### 5. 聊天、掷骰与跑团 LOG

- 房间聊天实时同步玩家发言、骰子结果和系统提示。
- 系统记录玩家进入、离开及主要房间事件。
- 骰子支持 `xdy+z` 和负修正表达式，例如 `2d6+3`、`1d20-1`。
- 提供常用骰面快捷输入，并播放与实际结果一致的总值滚动动画。
- 当至少半数骰子投出最大值时判定大成功，至少半数投出 1 时判定大失败。
- 跑团 LOG 可以按发言、骰子和系统消息筛选及搜索，并可导出为 Markdown 文件。

### 6. 背包与 SN 道具库

- 房间背包绑定角色卡，包含武器、防具、武器配件、防御配件、投掷物和消耗品。
- 玩家可以在游戏过程中添加或删除自己背包内的物品。
- 背包物品可以赠与同房间其他已绑定角色。
- 背包变化持续保存到角色卡，不会因退出房间丢失。
- SN 拥有独立道具库，可以加入规则库中的投掷物、消耗品和配件，也可以手动添加道具。
- SN 可以将道具库物品直接分配给指定玩家角色。

## 六、战术棋盘与 TOKEN

### 1. 棋盘视图

- 每位用户可以独立切换 2D 或 2.5D 显示，不要求同房间成员使用相同视角。
- 2.5D 模式使用轻微透视和高台侧面表现，同时保持 TOKEN 位于格子中心。
- 支持滚轮缩放、鼠标拖动画布和平移视野。
- 鼠标坐标锁定效果保留，便于对齐格子与战术位置。
- 2.5D 模式不会重复叠加 2D 网格线。

### 2. 地图与场景素材

- SN 可以上传地图图片，并调整名称、宽高、缩放、图层、透明度、锁定和位置。
- 地图素材可以调整上下图层顺序。
- 场景层独立覆盖在地图层上方，可随时显示或隐藏。
- 场景层支持背景图、角色立绘和 NPC；可从现有角色资源选择，也可上传自定义图片。
- 场景素材支持缩放、坐标、尺寸、透明度、显隐、锁定和图层调整。

### 3. 普通 TOKEN

- 可以从玩家角色创建 TOKEN，也可以上传自定义图片创建普通 TOKEN。
- 支持 TOKEN 图片裁切、缩放、位置调整和样式设置。
- TOKEN 正中心吸附到方格中心，而不是网格交点。
- TOKEN 具有上、下、左、右四个方向属性，可以使用方向键调整。
- 支持玩家、敌方和中立阵营，以及普通和高危等状态显示。
- 高危敌方单位显示十字坐标和圆形十字准星锁定效果。

### 4. 预设动画 TOKEN

- 支持敌方预设与友方干员预设两类 Spine 动画资源。
- 选择框使用头像或静态动画截图预览，避免同时播放大量预览动画。
- 支持按敌我阵营、分类和名称筛选预设。
- 支持批量创建，同一预设可使用加减按钮调整数量，并自动分散放置避免重叠。
- 预设 TOKEN 平时播放待机动画，拖动时播放移动动画，周围八格出现敌对单位时切换攻击动画。
- 动画切换复用同一渲染实例，减少切换时闪烁。
- 朝左时模型水平翻转，朝右时恢复；朝上和朝下不改变模型镜像。
- 未选中时仅显示动画模型，选中后才显示方向环、属性条和操作组件。
- 拖动轨迹、移动动画和最终位置会同步给房间内其他用户。

### 5. TOKEN 属性与战术信息

- TOKEN 管理分为“TOKEN 样式”和“TOKEN 属性”两个可切换面板。
- 属性包含生命值、技力和耐力，并分别使用红色、浅绿色和蓝色状态条。
- 选中 TOKEN 后，下方信息框展示姓名、控制权和三项资源的当前值/上限。
- 绑定角色卡的 TOKEN 可以按角色职业显示攻击范围格，SN 或控制者可在 TOKEN 管理中开关。
- 玩家与敌方 TOKEN 会根据阵营关系判断预设攻击动画目标。

### 6. 选择、批量操作与快捷键

- 点击空白处拖动可以框选多个 TOKEN，不会自动打开单个 TOKEN 管理页。
- SN 可以对框选目标统一移动、调整方向、缩放、复制和删除。
- `W/A/S/D`：按一个网格移动当前有权限的 TOKEN。
- `方向键`：调整 TOKEN 朝向。
- `Ctrl + +` / `Ctrl + -`：增大或减小当前选中目标。
- `Delete`：删除当前选中目标。
- `Ctrl + C`：复制当前选中 TOKEN、地图或场景素材。
- `Ctrl + V`：粘贴到当前鼠标所在位置。

### 7. 网格与地形模板

- SN 可以调整网格大小、线宽、颜色、实线/虚线样式、显示状态和吸附网格开关。
- 可以从地图编辑器载入已保存地形，也可以清除当前地形。
- 高台和地形图案会同时显示在 2D 与 2.5D 棋盘中。

## 七、地图编辑器

- 地图编辑器独立于游戏房间和角色卡，地形模板归当前用户管理。
- 默认编辑范围为 25×25 格。
- 高度分为地面 H0 与统一高度的高台 H1。
- 支持按住拖动连续绘制高台、框选多个格子和拖动画布。
- 支持滚轮缩放、加减缩放按钮和重置视图。
- 支持全选、追加选择、取消选择、批量设为 H0/H1 和批量清除效果。
- 支持新建、保存、另存为、载入和删除地形模板。
- 放大时显示地形图案，缩小时使用更简化的单元格表现以控制性能。
- 当前可编辑的地形字段包括：陷坑、草丛、热泵通道、活性源石、腐蚀地面、岩浆喷射处、沼泽、冰面、深水区、通道入口、通道出口、医疗地块和防御地块。
- 地形图案保留各自视觉特征，例如陷坑的危险齿圈、岩浆喷射处的火焰与液滴、活性源石的黑色结晶、黄色末端及漂浮粉尘。

## 八、当前功能边界

- 地形效果已经具备字段、编辑、保存和棋盘视觉展示，但多数效果尚未自动参与移动、伤害、抗性和状态结算，现阶段由玩家与 SN 按规则处理。
- 召唤物已经预留角色卡数据接口，完整创建与房间行为仍待后续扩展。
- 低配机模式会主动减少动画表现；大量同时播放的预设 TOKEN 仍可能受到设备性能影响。
- 网站目前以 V1.2 角色卡、战斗职业表、源石技艺表和速查表作为主要规则数据来源，后续规则变更需要同步更新对应解析与计算逻辑。

## 九、推荐使用流程

1. 注册并登录网站。
2. 新建角色卡，或在角色卡编辑页导入 V1.2 Excel 角色卡。
3. 补充职业、技能、特质、源石技艺、装备、感染与人物档案，并确认自动计算结果。
4. SN 在地图编辑器中准备地形模板，并创建空白房间或恢复历史存档。
5. 玩家使用邀请码加入房间，可在进入前绑定角色，也可先无角色进入。
6. SN 应用地图与地形，创建普通或预设 TOKEN，设置控制权、场景层和道具库。
7. 玩家通过棋盘、聊天、掷骰、背包和角色卡共同进行游戏。
8. 游戏过程中由 SN 手动保存关键节点；所有人退出后，房间转入存档，后续可继续开启。
